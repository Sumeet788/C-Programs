#include<stdio.h>
#include<conio.h>

int main()
{
    int side;

    printf("Enter your side of square: ");
    scanf("%d", &side);

    printf("Area of a square: %d", side*side);
    return 0;
}